spring:
  application:
    name: store

stripe:
  apiUrl: https://api.stripe.com
  enabled: true
  timeout: 1000
  supported-currencies: EUR,USD,INR

email:
  host: smtp://gmail.com
  port: 7000

payment-gateway: paypal